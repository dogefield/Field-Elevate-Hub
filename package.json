{
  "name": "field-elevate-platform",
  "version": "1.0.0",
  "description": "AI-Powered Hedge Fund Platform",
  "private": true,
  "scripts": {
    "start": "cd mcp-hub && npm start",
    "build": "cd mcp-hub && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}